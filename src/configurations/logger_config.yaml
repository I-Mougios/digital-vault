---
version: 1
disable_existing_loggers: false

formatters:
  dict_formatter:
    "()": log_utils.DictFormatter


handlers:
  mongo_handler:
    class: log_utils.MongoLogger
    formatter: dict_formatter

loggers:
  root:
    level: DEBUG

  mongo_logger:
    level: INFO
    handlers:
      - mongo_handler




